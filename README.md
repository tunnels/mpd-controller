It's a colection of helper scripts for mpd/mpc

'add random album' and 'add random songs' both require shuf (part of gnu/coreutils: OS X users can get it through homebrew with, "brew install coreutils")